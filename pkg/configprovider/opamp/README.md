# OpAMP config provider

A configuration provider that can connect to a OpAMP server for remote configuration management.

This allows the following invocation:

```bash
./otelcol --config "opamp:wss://example.com:8081"
```
