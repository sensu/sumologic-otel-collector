receivers:
  monitoringjobs:
    jobs:
      - name: test
        schedule:
          interval: 10
        exec:
          command: "echo foobar"

exporters:
  logging:
    logLevel: debug

service:
  pipelines:
    metrics:
      receivers: [monitoringjobs]
      exporters: [logging]
    logs:
      receivers: [monitoringjobs]
      exporters: [logging]
